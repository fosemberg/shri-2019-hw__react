import React from 'react';
import './File-Icon_type_readme.scss';

const FileIcon_type_readme = (props) => (
  <div className="File-Icon_type_readme">{props.children}</div>
);

export default FileIcon_type_readme;