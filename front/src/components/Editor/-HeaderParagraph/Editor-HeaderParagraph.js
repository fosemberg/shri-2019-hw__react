import React from 'react';
import './Editor-HeaderParagraph.scss';

const EditorHeaderParagraph = (props) => (
  <div className="Editor-HeaderParagraph">{props.children}</div>
);

export default EditorHeaderParagraph;