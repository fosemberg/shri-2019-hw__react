import React from 'react';
import './Editor-Row_color_added.scss';

const EditorRow_color_added = (props) => (
  <div className="Editor-Row_color_added">{props.children}</div>
);

export default EditorRow_color_added;