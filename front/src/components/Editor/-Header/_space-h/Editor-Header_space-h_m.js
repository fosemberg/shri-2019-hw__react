import React from 'react';
import './Editor-Header_space-h_m.scss';

const EditorHeader_spaceH_m = (props) => (
  <div className="Editor-Header_space-h_m">{props.children}</div>
);

export default EditorHeader_spaceH_m;