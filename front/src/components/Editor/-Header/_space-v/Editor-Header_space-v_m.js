import React from 'react';
import './Editor-Header_space-v_m.scss';

const EditorHeader_spaceV_m = (props) => (
  <div className="Editor-Header_space-v_m">{props.children}</div>
);

export default EditorHeader_spaceV_m;