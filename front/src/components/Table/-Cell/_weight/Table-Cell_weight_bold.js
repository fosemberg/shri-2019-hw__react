import React from 'react';
import './Table-Cell_weight_bold.scss';

const TableCell_weight_bold = (props) => (
  <div className="Table-Cell_weight_bold">{props.children}</div>
);

export default TableCell_weight_bold;