import React from 'react';
import './Text_color_accented.scss';

const Text_color_accented = (props) => (
  <div className="Text_color_accented">{props.children}</div>
);

export default Text_color_accented;