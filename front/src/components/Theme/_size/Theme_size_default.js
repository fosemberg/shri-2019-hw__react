import React from 'react';
import './Theme_size_default.scss';

const Theme_size_default = (props) => (
  <div className="Theme_size_default">{props.children}</div>
);

export default Theme_size_default;