import { withBemMod } from '@bem-react/core';
import { IFileIcon } from '../index';
import './File-Icon_type_code.scss';

export const FileIconTypeCode = withBemMod<IFileIcon>('File-Icon', {
  type: 'code',
});
